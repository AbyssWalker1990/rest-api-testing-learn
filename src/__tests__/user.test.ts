describe('user', () => {
  // user registration

  describe('user registration', () => {
    describe('given the username and password are valid', () => {
      it('shouls retuen the user payload', () => {

      })
    })

    describe('given the passwords do not match', () => {
      it('should return a 400', () => {

      })
    })

    describe('given the user service throws', () => {
      it('should return 409 error', () => {

      })
    })
  })
    // the username password get validation
    // verify that the passwords must match
    // verify that the handler handles any errors

  // creating a user session
    // a user can log in with a valid email and password
})